<!-- @note: what-we-did-not-cover -->
> Do:
> - Keep it short — naming these pre-empts "why didn't you show X"
> - Channels and computer use are research previews; /goal needs a trusted folder with hooks enabled
> - Say "not covered" — do not promise they run on every machine

Say:
- Channels: an MCP server pushes events into your open session; opt in with `--channels` — https://code.claude.com/docs/en/channels
- /goal: Claude keeps working until a small model judges the condition met or impossible — https://code.claude.com/docs/en/goal
- Computer use: a built-in MCP server, enabled in /mcp; macOS only, Pro or Max plan — https://code.claude.com/docs/en/computer-use

<!-- @note: context-is-king-you-push-it-you-own-it -->
> Do:
> - The close — two lines, no diagram

Say:
- One throughline: careless context fills the window with noise and the agent drifts
- Context engineered as a resource is the biggest lever you have
- Skills, subagents, hooks, MCP, workflows: every row of the map managed that one constraint
- You push the agent's context. You own what happens because of it.
